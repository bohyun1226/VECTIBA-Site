# vectiba.com — 현재 상태 (2026-07-13 기준)

다음 세션은 이 문서부터 읽고 이어서 작업. 소스는 이 폴더(`~/vectiba-dev/VECTIBA-Site`, 영구). 클론: `gh repo clone bohyun1226/VECTIBA-Site`.

## 배포
- GitHub Pages, PUBLIC repo `bohyun1226/VECTIBA-Site`, branch `main` root, 도메인 vectiba.com (CNAME), HTTPS.
- 빌드: `python3 _src/build.py` → `_src/tpl.html`(HTML) + build.py 안 EN 사전 + `i18n/<lang>.json` 합쳐 `index.html` 생성. → `git commit` → `git push` (약 1분 뒤 반영).
- 캐시: HTML `max-age=600`(10분). 서비스워커 없음. **처음 오는 사람은 즉시 새 화면**. 대표 본인 기기만 브라우저가 최대 10분 예전 걸 보여줄 수 있음(시크릿/다른 기기면 즉시).
- 브랜드: 크림 #F8F5EE · 딥그린 #135A4B · 골드 #BE8B39 · 에메랄드 #0B7A5A(딜러 데모) · Poppins · 실제 V로고.

## 페이지 구조 (확정, 국문 기준)
메뉴 5개: **벡티바? · 셀러 · 딜러 · 팀 · 문의**
1. **벡티바?**(히어로, 크림) — 3축: "셀러도, 딜러도, 운송사도 모두 좋아지는 구조" (정본). 오른쪽 박스 🚗셀러 🤝딜러 🚢운송사.
2. **셀러 이점**(흰색) — 대표님 셀러 원문(내 차 너무 싸게? / 나라별 가격 / 가장 높게 쳐주는 딜러에게 비대면) + 문제 3카드 + 셀러 **폰 데모**(`demo/`).
3. **딜러 이점**(크림) — 대표님 딜러 원문(전 세계 매물 접근 / AI검수 / 물류 / 순이익) + 딜러 **PC 데모**(`dealer/`).
4. **팀**(딥그린 배너) — 대표님 1인칭 스토리(8년·15개국·연1,000대·월2대→연1,000대) + 사진 `img/dustin.jpg`.
5. **문의**(흰색) — WhatsApp·LinkedIn·이메일 + **딜러·파트너 신청**(팝업 모달, GitHub 로그인 스타일) + 주소(두바이 Jebel Ali / 서울 Seocho, 둘 다 영어표기).

## 데모
- `demo/` 셀러 폰: AI 대화 인테이크(음성·사진), **16개 언어**. 낙찰=두바이/UAE(모로코 아님), 나라이름 다국어. 따뜻한 크림 #F4ECDA.
- `dealer/` 딜러 PC: 에어비앤비식 카드그리드(사이드메뉴X), **밀봉경매**(건수만·낙찰가 표시X), BMW G30 상세(사진 작게+라이트박스), **AI 도착가 계산기**(부가비용 합계 접기/펼치기 + 로드탁송 캐리어/기사 + 화폐선택 + **당일환율** open.er-api.com), [입찰하기]. 에메랄드 헤더 + 화이트/샌드. **ko/en 2개 언어만**(16개 확장 대기).
- 두 데모 모두 사이트 언어 따라 첫 화면부터 그 언어로 로드(하드코딩 en 제거, applyLang이 src 지정).
- 차 사진: `dealer/img/` 14대 정면 Wikimedia CC(도요타·벤츠·BMW·G클래스·랜드크루저+프랑스 푸조/르노), 출처 `dealer/img/ATTRIBUTIONS.txt`. **크롤링·캡처·브랜드 프레스사진 금지**(운영 땐 셀러가 실사진 올림).

## 가입신청 폼 저장 (진행 중)
- 폼=문의 섹션 팝업. 지금은 **이메일 접수**(mailto 폴백). Supabase 연결 시 DB 자동저장.
- `_src/tpl.html`의 `LEAD_CFG = { url:'', key:'' }` 에 **Supabase Project URL + anon(public) 키** 넣으면 DB 저장으로 전환. (service_role 키는 절대 넣지 말 것.)
- 테이블 SQL: `supabase/leads.sql` (insert-only RLS). 대표 Supabase 프로젝트명 "Vectiba-app", leads 테이블 생성 완료. **남은 것: URL+anon 키 받아 LEAD_CFG에 넣고 배포.**

## 남은 일 (다음 세션)
1. **Supabase URL+anon 키** 받아 폼→DB 연결.
2. **히어로 핵심카피 교체 완료(2026-07-13)**: "만나지 못했던 사람들을, AI 기술로 이어줍니다" (대표님 확정 문구) — 16개 언어 전부 반영 완료. 같이 밀려있던 누락 키 11개(팀/문의 섹션)도 16개 언어 전부 채움.
   - 단, 이건 "이번에 새로 생긴 구멍"만 메운 것. 그 이전부터 누적된 **다른 키들의 번역 드리프트**(국/영문은 여러 차례 고쳤는데 나머지 14개 언어는 예전 버전 그대로인 키들)는 아직 전수 점검 안 됨 — 필요시 EN 소스와 언어별 파일을 키 단위로 diff해서 확인.
   - **딜러 데모 16개 언어** 확장은 아직 미완(ko/en만).
3. 추가 레이아웃/문구 요청 반영.

## 모바일 (2026-07-13 개선)
- **딜러 데모 치명적 버그 수정**: `dealer/index.html`의 `<style>`이 안 닫혀 있어서 캐시 없는 첫 접속(모바일 첫 방문이 전형적)에 페이지 전체가 깨졌던 문제 해결. 안드로이드·아이폰 둘 다에서 "매우 불편하다"던 원인이 이거였음.
- **모바일 내비게이션 추가**: 940px 이하에서 메뉴 링크가 전부 사라지고 언어선택만 남던 것을, 햄버거 버튼(`#hamBtn`) + 드롭다운(`#mobileMenu`, 벡티바?/셀러/딜러/팀/문의 5개 스택형 링크)으로 교체. RTL(아랍어/페르시아어)도 자동 대응.
- 위 작업 중 `nav_seller`/`nav_buyer` 14개 언어가 옛 영문("Service (sellers/buyers)") 그대로였던 것도 같이 발견해 수정(데스크톱 내비에선 안 보이던 게 모바일 메뉴로 드러남).
- 그 외 히어로/셀러·딜러 섹션/팀/문의폼/모달은 모바일에서 이미 정상.

## 참고: 동시 세션 주의
이 레포는 같은 작업 디렉토리(`~/vectiba-dev/VECTIBA-Site`)에서 다른 세션이 동시에 커밋·푸시하는 경우가 있었음(2026-07-13 세션 중 여러 차례 확인). 작업 시작 전 항상 `git log --oneline -5`, `git status`로 최신 상태 확인하고, 로컬에 낯선 uncommitted 변경이 있으면 지우지 말고 같이 커밋에 포함시킬 것(대개 다른 세션이 의도적으로 남긴 변경임).

## 대표님 작업 규칙 (반드시)
- **대표님 원문 그대로**(가볍게만 다듬기), 내가 다시 쓰지 않음. 재검 안 받고 바로 배포.
- **짧게, 한 번에 한 가지**. 캐시 얘기 금지. 시킨 것만(추가 X). 부정문 금지.
